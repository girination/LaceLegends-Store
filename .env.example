# Supabase example environment file
# Copy to `.env.local` and replace the placeholder values with your project's keys.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-anon-key

# Server-only key: use this only in server-side code (API routes, server functions)
# Do NOT expose this to client-side bundles or commit it anywhere.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
